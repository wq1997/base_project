import { Input, Select } from "antd";

const SearchInput = ({
    label = undefined,
    value = undefined,
    placeholder = undefined,
    inputWidth = 200,
    type = "input",
    options = [],
    fieldNames = {
        label: "displayName",
        value: "name",
    },
    onChange = () => {},
}) => {
    return (
         
    );
};

export default SearchInput;
