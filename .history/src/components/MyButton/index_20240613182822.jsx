import { Button } from "antd";
import { useState } from "react";

const Index = ({ active, color, text }) => {
    return <div style={{
        borderCo
    }}>{text}</div>;
};

export default Index;
